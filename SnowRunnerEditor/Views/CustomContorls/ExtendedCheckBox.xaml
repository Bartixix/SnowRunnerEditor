<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SnowRunnerEditor.Views.CustomContorls.ExtendedCheckBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnowRunnerEditor.Views.CustomContorls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Height="auto" x:Name="ContentGrid">
        <Grid.ColumnDefinitions >
            <ColumnDefinition MaxWidth="32"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Column="0" x:Name="Box" Margin="0 0 10 0"/>
        <TextBlock Grid.Column="1" x:Name="Lbl" Margin="0 7.5 0 0" Text="{x:Bind Label}" Width="auto"/>        
    </Grid>
</UserControl>
